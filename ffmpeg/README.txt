For the Windows EXE build, place both official binaries here:

ffmpeg/bin/ffmpeg.exe
ffmpeg/bin/ffprobe.exe

Dubora also checks the system PATH in development mode. imageio-ffmpeg may provide ffmpeg as a development fallback, but ffprobe must still be bundled or installed.
